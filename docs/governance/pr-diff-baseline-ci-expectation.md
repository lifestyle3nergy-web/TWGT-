# CI expectation

Opening the repair PR should exercise the same pull-request checkout path that exposed the original defect. Repository Governance is therefore the primary regression signal; the remaining CI/security checks establish that the isolated validator repair has no collateral effect.
