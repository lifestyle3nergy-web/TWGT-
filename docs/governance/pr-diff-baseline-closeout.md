# Closeout requirements

After an eventual approved merge, verify the post-merge `main` CI cycle and record whether Repository Governance passes on subsequent pull requests. If the original failure pattern recurs, treat the repair as incomplete and reopen diagnosis rather than bypassing the check.
