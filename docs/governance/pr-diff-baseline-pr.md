# Pull request intent

Open this branch against `main` as an isolated governance repair. The PR should remain unmerged while GitHub executes the required checks and an independent reviewer evaluates the exact head.
