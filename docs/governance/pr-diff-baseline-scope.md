# Repair scope

The candidate is intentionally isolated to repository-governance diff calculation and its evidence:

- repository contract validator
- regression test
- ADR
- validation documentation

It changes no runtime API, dependencies, lockfile, provider activation, database schema, deployment configuration, or application capability.
