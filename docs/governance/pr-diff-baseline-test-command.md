# Governance regression test command

Run the synthetic pull-request merge regression independently with:

```sh
node --test scripts/validate-repository-contract.test.mjs
```

This is additional evidence. It does not replace the repository's standard `npm test` gate.
