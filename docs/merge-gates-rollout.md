# Merge Gates Rollout Boundary

This branch is an isolated governance proposal. Merging it adds workflow enforcement code but does not itself activate or alter a GitHub repository ruleset.

After the PR is green, observe the exact emitted check names and verify merge-group coverage for all security workflows. Only then should repository administrators create or activate the `main-fail-closed` ruleset.
